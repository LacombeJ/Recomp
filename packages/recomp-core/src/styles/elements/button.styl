@import '../common/colors';

recomp-button() {

  BACKGROUND_COLOR_TOP = lighten(RECOMP_PRIMARY, 10%);
  BACKGROUND_COLOR_BOT = RECOMP_PRIMARY;
  
  DEFAULT_BACKGROUND = RECOMP_PRIMARY;
  DEFAULT_BORDER = lighten(RECOMP_PRIMARY, 10%);

  PLAIN_BACKGROUND = #a3a3a3;
  PLAIN_BORDER = #9ca5a4;

  WARN_BACKGROUND = #852c33;
  WARN_BORDER = #e44c4c;

  .recomp-button {
    color: rgba(255, 255, 255, 0.9);
    background-color: rgba(DEFAULT_BACKGROUND, 0.7);
    border: 1px solid DEFAULT_BORDER;

    border-radius: 2px;
    box-shadow: inset rgba(0, 0, 0, 0.0) 0 4px 8px, rgba(0, 0, 0, 0.0) 0px 4px 8px;
    // box-shadow: rgba(60, 80, 100, 0.2) 0px 1px 1px;
    cursor: pointer;
    display: inline-block;
    line-height: 1.5em;
    padding: 8px 16px;
    text-align: center;
    white-space: nowrap;
    transition: all .2s cubic-bezier(0.645, 0.045, 0.355, 1);
    
    user-select: none;
    touch-action: manipulation;

    &:hover {
      box-shadow: inset rgba(0, 0, 0, 0.0) 0 4px 8px, rgba(0, 0, 0, 0.9) 0px 4px 8px;
      // box-shadow: rgba(0, 0, 0, 0.9) 0px 4px 8px;
      background-color: rgba(DEFAULT_BACKGROUND, 0.8);
      border: 1px solid lighten(DEFAULT_BORDER, 20%);
    }

    &.plain {
      color: rgba(255, 255, 255, 0.9);
      background-color: rgba(PLAIN_BACKGROUND, 0.3);
      border: 1px solid PLAIN_BORDER;
      &:hover {
        background-color: rgba(PLAIN_BACKGROUND, 0.6);
        border: 1px solid lighten(PLAIN_BORDER, 20%);
      }
    }

    &.warn {
      color: rgba(255, 255, 255, 0.9);
      background-color: rgba(WARN_BACKGROUND, 0.7);
      border: 1px solid WARN_BORDER;
      &:hover {
        background-color: rgba(WARN_BACKGROUND, 0.8);
        border: 1px solid lighten(WARN_BORDER, 20%);
      }
    }

    &:active {
      transition-duration: .1s;
      // box-shadow: rgba(0, 0, 0, 0.15) 0 2px 4px inset, rgba(0, 0, 0, 0.4) 0 1px 1px;
      box-shadow: inset rgba(0, 0, 0, 0.3) 0 4px 8px, rgba(0, 0, 0, 0.9) 0px 4px 8px;
    }

    &:disabled {
      cursor: not-allowed;
      opacity: .6;
      &:active {
        pointer-events: none;
      }

      &:hover {
        box-shadow: none;
      }
    }
  }

}